// AUTO-GENERATED BY @swiftstatetree/sdk codegen.
// Do not edit this file directly.

export type Acceleration2 = { v: IVec2 }
export type BuyUpgradeAction = { upgradeID: string }
export type BuyUpgradeResponse = { newCookies: number; newCookiesPerSecond: number; success: boolean; upgradeLevel: number }
export type ClickCookieEvent = { amount: number }
export type CookieGameState = { players: { [key: string]: CookiePlayerPublicState }; privateStates: { [key: string]: CookiePlayerPrivateState }; ticks: number; totalCookies: number }
export type CookiePlayerPrivateState = { totalClicks: number; upgrades: { [key: string]: number } }
export type CookiePlayerPublicState = { cookies: number; cookiesPerSecond: number; name: string }
export type CounterState = { count: number }
export type DeterministicMathDemoState = { directVector: IVec2; playerAccelerations: { [key: string]: Acceleration2 }; playerPositions: { [key: string]: Position2 }; playerVelocities: { [key: string]: Velocity2 } }
export type IVec2 = { x: number; y: number }
export type IncrementAction = { [key: string]: any }
export type IncrementResponse = { newCount: number }
export type Position2 = { v: IVec2 }
export type StateDiff = { patches: { op: string; path: string; value?: { [key: string]: any } }[] }
export type Velocity2 = { v: IVec2 }

// DeterministicMath conversion helpers
// Fixed-point scale factor (matches Swift FixedPoint.scale)
export const FIXED_POINT_SCALE = 1000

/**
 * Convert IVec2 to Float coordinates.
 * @param vec - The fixed-point integer vector
 * @returns Object with x and y as float numbers
 * @example
 * const vec: IVec2 = { x: 1500, y: 2300 }
 * const float = IVec2ToFloat(vec) // { x: 1.5, y: 2.3 }
 */
export function IVec2ToFloat(vec: IVec2): { x: number; y: number } {
  return { x: vec.x / 1000, y: vec.y / 1000 }
}

/**
 * Convert Float coordinates to IVec2.
 * @param x - X coordinate as float
 * @param y - Y coordinate as float
 * @returns IVec2 with quantized integer values
 * @example
 * const vec = FloatToIVec2(1.5, 2.3) // { x: 1500, y: 2300 }
 */
export function FloatToIVec2(x: number, y: number): IVec2 {
  return { x: Math.round(x * 1000), y: Math.round(y * 1000) }
}

/**
 * Convert Position2 to Float coordinates.
 * @param pos - The Position2 value
 * @returns Object with x and y as float numbers
 */
export function Position2ToFloat(pos: Position2): { x: number; y: number } {
  return IVec2ToFloat(pos.v)
}

/**
 * Convert Float coordinates to Position2.
 * @param x - X coordinate as float
 * @param y - Y coordinate as float
 * @returns Position2 with quantized integer values
 */
export function FloatToPosition2(x: number, y: number): Position2 {
  return { v: FloatToIVec2(x, y) }
}

/**
 * Convert Velocity2 to Float coordinates.
 * @param vel - The Velocity2 value
 * @returns Object with x and y as float numbers
 */
export function Velocity2ToFloat(vel: Velocity2): { x: number; y: number } {
  return IVec2ToFloat(vel.v)
}

/**
 * Convert Float coordinates to Velocity2.
 * @param x - X coordinate as float
 * @param y - Y coordinate as float
 * @returns Velocity2 with quantized integer values
 */
export function FloatToVelocity2(x: number, y: number): Velocity2 {
  return { v: FloatToIVec2(x, y) }
}

/**
 * Convert Acceleration2 to Float coordinates.
 * @param accel - The Acceleration2 value
 * @returns Object with x and y as float numbers
 */
export function Acceleration2ToFloat(accel: Acceleration2): { x: number; y: number } {
  return IVec2ToFloat(accel.v)
}

/**
 * Convert Float coordinates to Acceleration2.
 * @param x - X coordinate as float
 * @param y - Y coordinate as float
 * @returns Acceleration2 with quantized integer values
 */
export function FloatToAcceleration2(x: number, y: number): Acceleration2 {
  return { v: FloatToIVec2(x, y) }
}
