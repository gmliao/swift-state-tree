// AUTO-GENERATED BY @swiftstatetree/sdk codegen.
// Do not edit this file directly.

// Import DeterministicMath classes from SDK
// These classes automatically convert fixed-point integers to floats via getters
// @ts-ignore - Classes are used in type definitions below
import { IVec2, Angle, Position2, FIXED_POINT_SCALE } from '@swiftstatetree/sdk/core'

export { IVec2 }
export { Angle }
export { Position2 }
export { FIXED_POINT_SCALE }

// @ts-ignore - DeterministicMath classes (Position2) are used in type definitions
export type BaseState = { health: number; maxHealth: number; position: Position2; radius: number }
export type HeroDefenseState = { base: BaseState; currentTick: number; monsters: { [key: string]: MonsterState }; nextMonsterID: number; nextTurretID: number; players: { [key: string]: PlayerState }; score: number; turrets: { [key: string]: TurretState } }
// @ts-ignore - DeterministicMath classes (Angle, Position2) are used in type definitions
export type MonsterState = { health: number; id: number; maxHealth: number; pathProgress: number; position: Position2; reward: number; rotation: Angle; spawnPosition: Position2 }
// @ts-ignore - DeterministicMath classes (Position2) are used in type definitions
export type MoveToEvent = { target: Position2 }
// @ts-ignore - DeterministicMath classes (Position2) are used in type definitions
export type PlaceTurretEvent = { position: Position2 }
export type PlayAction = { [key: string]: any }
export type PlayResponse = { newScore: number }
// PlayerID is a Swift struct with rawValue: String property
export type PlayerID = { rawValue: string }
// @ts-ignore - DeterministicMath classes (Position2) are used in type definitions
export type PlayerShootEvent = { from: Position2; playerID: PlayerID; to: Position2 }
// @ts-ignore - DeterministicMath classes (Angle, Position2) are used in type definitions
export type PlayerState = { health: number; lastFireTick: number; maxHealth: number; position: Position2; resources: number; rotation: Angle; targetPosition: Optional<Position2>; weaponLevel: number }
export type ShootEvent = { [key: string]: any }
export type StateDiff = { patches: { op: string; path: string; value?: { [key: string]: any } }[] }
// @ts-ignore - DeterministicMath classes (Position2) are used in type definitions
export type TurretFireEvent = { from: Position2; to: Position2; turretID: number }
// @ts-ignore - DeterministicMath classes (Angle, Position2) are used in type definitions
export type TurretState = { id: number; lastFireTick: number; level: number; ownerID: Optional<PlayerID>; position: Position2; rotation: Angle }
// @ts-ignore - DeterministicMath classes (Angle) are used in type definitions
export type UpdateRotationEvent = { rotation: Angle }
export type UpgradeTurretEvent = { turretID: number }
export type UpgradeWeaponEvent = { [key: string]: any }

// Generic Optional type for all Optional<T> usages
export type Optional<T> = T | null | undefined

// Angle conversion helpers (for degrees/radians conversion)
// Note: Angle class automatically handles fixed-point to float conversion.
// These helpers only convert between degrees and radians.

/**
 * Convert Angle to radians.
 * @param angle - The Angle instance (degrees getter automatically returns float)
 * @returns Angle in radians as float
 * @example
 * const angle = new Angle(45000, true) // fixed-point integer
 * const rad = AngleToRadians(angle) // ≈ 0.785 (π/4)
 */
export function AngleToRadians(angle: Angle): number {
  return angle.degrees * Math.PI / 180
}

/**
 * Convert Angle to degrees (float).
 * @param angle - The Angle instance (degrees getter automatically returns float)
 * @returns Angle in degrees as float
 * @example
 * const angle = new Angle(45000, true) // fixed-point integer
 * const deg = AngleToDegrees(angle) // 45.0
 */
export function AngleToDegrees(angle: Angle): number {
  return angle.degrees
}

/**
 * Convert radians to Angle.
 * @param radians - Angle in radians as float
 * @returns Angle instance (degrees setter automatically converts to fixed-point)
 * @example
 * const angle = RadiansToAngle(Math.PI / 4) // new Angle(45.0, false)
 */
export function RadiansToAngle(radians: number): Angle {
  const degrees = radians * 180 / Math.PI
  return new Angle(degrees, false) // false = float input
}

/**
 * Convert degrees to Angle.
 * @param degrees - Angle in degrees as float
 * @returns Angle instance (degrees setter automatically converts to fixed-point)
 * @example
 * const angle = DegreesToAngle(45.0) // new Angle(45.0, false)
 */
export function DegreesToAngle(degrees: number): Angle {
  return new Angle(degrees, false) // false = float input
}
